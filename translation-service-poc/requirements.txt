fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
Pillow>=10.4,<13
numpy>=1.26,<3
# rapidocr_onnxruntime declares opencv-python (not the headless wheel), so use
# the same distribution to avoid installing two competing cv2 packages.
opencv-python>=4.10,<5
opencc-python-reimplemented>=0.1.7,<1
# 1.2.3 is the current release on PyPI and the version used for the POC.
rapidocr_onnxruntime>=1.2.3,<1.3
